@page "/dokumentace"
@inject IConfiguration Config

<h1>Dokumentace</h1>

<div class="doc">
    <p>Serverová admin konzole nástroje <strong>USB Guardian</strong> — sledování paměťových
    médií na firemních stanicích (NIS2 / ISO 27001). Agreguje data ze všech agentů a spravuje
    inventář stanic.</p>

    <h2>Rozcestník dokumentace</h2>
    <p class="muted">Samostatné tisknutelné stránky (🖨 v light režimu) — žádné externí odkazy.
    Grafické výstupy mají v hlavičce přepínač <strong>Čeština / English</strong>.</p>

    <h3>Pro vedení a prezentaci</h3>
    <ul class="linklist">
        <li><a href="management-summary.html" target="_blank" rel="noopener">📄 Shrnutí pro vedení (A4)</a> —
            jedna strana na výšku, k vytištění: co to je, proč to je, kde jsme a co dál</li>
        <li><a href="how-it-works.html" target="_blank" rel="noopener">▶ Jak to funguje (animace)</a> —
            tok informací krok po kroku, 15 kroků od zapojení média po nasazení</li>
        <li><a href="mind-map.html" target="_blank" rel="noopener">🧠 Myšlenková mapa</a> —
            z čeho se systém skládá a co k čemu patří</li>
        <li><a href="flowchart.html" target="_blank" rel="noopener">🔀 Vývojový diagram</a> —
            cesta jednoho média od zapojení po auditní stopu, včetně rozhodovacích bodů</li>
    </ul>

    <h3>Technická dokumentace</h3>
    <ul class="linklist">
        <li><a href="dokumentace/architektura">Architektura</a> · <a href="dokumentace/architektura-en">EN</a>
            — datový tok, komponenty, bezpečnostní vrstvy, deník provozu</li>
        <li><a href="dokumentace/handoff">HANDOFF</a> · <a href="dokumentace/handoff-en">EN</a>
            — předávka projektu + živý stav</li>
        <li><a href="dokumentace/readme">README</a> · <a href="dokumentace/readme-en">EN</a>
            — funkční přehled, komponenty, konfigurace, nasazení</li>
        <li><a href="dokumentace/auto-deploy">Nastavení auto-enrollmentu</a> · <a href="dokumentace/auto-deploy-en">EN</a>
            — deploy účty (gMSA), GPO, úlohy na app serveru</li>
        <li><a href="dokumentace/oponentura">Oponentura (technická)</a> · <a href="dokumentace/oponentura-en">EN</a>
            — obhajoba návrhových rozhodnutí, hrozbový model, omezení a rizika</li>
        <li><a href="dokumentace/komercni">Oponentura (komerční)</a> · <a href="dokumentace/komercni-en">EN</a>
            — posudek komerčního potenciálu + reakce autora</li>
        <li>Provozní nápověda → níže na této stránce</li>
    </ul>

    <h2>Přístup</h2>
    <ul>
        <li>Autentizace <strong>Windows (Kerberos)</strong>. Dovnitř jen členové AD skupin
            uvedených v Nastavení (<code>AdminGroups</code>) nebo účty na whitelistu
            (<code>AllowedUsers</code>).</li>
        <li>Pro <strong>tiché přihlášení</strong> bez dialogu choď přes <strong>hostname</strong>
            serveru (ne přes IP) — IP prohlížeč nebere jako intranet.</li>
    </ul>

    <h2>Přehled</h2>
    <p>Incidenty (připojení neschválených médií) za posledních 30 dní — počty Blokováno /
    Varování a tabulka posledních událostí včetně identifikátorů média
    (<strong>VID / PID / sériové číslo</strong>) — to jsou hodnoty pro whitelist.</p>

    <h2>Stanice</h2>
    <p>Inventář počítačů z Active Directory (viz AD sync). Dlaždice filtrují tabulku —
    kolik stanic <strong>hlásí agenta</strong> a kterým <strong>agent chybí</strong>
    (= seznam pro nasazení). Tlačítkem <em>Aktualizovat z AD</em> spustíš sync na vyžádání.</p>

    <h2>AD sync</h2>
    <p>Server v intervalu (Nastavení → AD sync) načte počítače z domény a zapíše je do evidence.
    Klíčem je <strong>hostname</strong> (ne IP — stanice mají dynamické adresy). Nepřepisuje data
    hlášená agentem (poslední kontakt, verze agenta). Doménu bere automaticky podle serveru
    (nic se nezadává natvrdo).</p>

    <h2>Jak schválit médium (whitelist)</h2>
    <p>Stačí <strong>sériové číslo</strong> — VID/PID i název se dotáhnou z incidentů (i zpětně, až se
    médium na síti objeví). Po každé změně katalogu konzole sama <strong>vydá a podepíše</strong> novou verzi
    whitelistu interním RSA klíčem a agenti si ji do ~2 minut stáhnou; klient je tak <strong>1:1 kopie</strong>
    serveru. Nový záznam je nejdřív neaktivní — aktivuje se zaškrtnutím <em>Aktivní</em>.</p>

    <h2>Kontroly</h2>
    <p>Health checks serveru i klientů. Seznam kontrol se ukáže dopředu a odškrtává se s průběžnými výsledky,
    takže je vidět, co se právě ověřuje. Výsledky jdou exportovat do CSV, HTML, PDF (tisk) i TXT. Na téže
    stránce je i <strong>plánovaný restart</strong> služeb.</p>

    <h2>Aktivita (deník provozu)</h2>
    <p>Do Přehledu se dostane jen to, co skončilo incidentem. <strong>Aktivita</strong> ukazuje zbytek provozu:
    heartbeaty (a co na ně server odpověděl), příjem dávek incidentů, publikace whitelistu, ruční nasazení
    a vyřazení stanice. Píše do ní <strong>API i konzole</strong>, takže se to čte jako jeden příběh.
    Filtry (období, úroveň, zdroj, hledání), režim <em>živě</em> s obnovou po 3 s a export CSV.</p>

    <h2>Tato instance</h2>
    <dl class="cfg">
        <dt>Endpoint</dt><dd class="mono">@(Config["Kestrel:Endpoints:Http:Url"] ?? "—")</dd>
        <dt>SQL Server / DB</dt><dd class="mono">@_sql / @_db</dd>
        <dt>Build</dt><dd class="mono">commit @AppInfo.Commit</dd>
    </dl>

    <p class="muted">Vše firemně specifické (server, doména, skupiny, účty) je v
    <code>appsettings.local.json</code>. Plná technická dokumentace: repozitář
    <code>usb-guardian</code> (README, docs/architecture.md).</p>
</div>

@code {
    private string _sql = "—";
    private string _db  = "—";

    protected override void OnInitialized()
    {
        var cs = Config.GetConnectionString("DefaultConnection") ?? "";
        foreach (var part in cs.Split(';', StringSplitOptions.RemoveEmptyEntries))
        {
            var kv = part.Split('=', 2);
            if (kv.Length != 2) continue;
            var k = kv[0].Trim().ToLowerInvariant();
            if (k is "server" or "data source") _sql = kv[1].Trim();
            if (k is "database" or "initial catalog") _db = kv[1].Trim();
        }
    }
}
